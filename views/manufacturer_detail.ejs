<!DOCTYPE html>
<html>
  <head>
    <title><%= title %> - <%= manufacturer.name %></title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
  </head>
  <body>
    <%- include('header'); -%>
    <div class="container">
      <div class="row s12">
        <h1><%= title %></h1>
        <a
          href="delete/<%= manufacturer._id %>"
          class="waves-effect waves-light btn blue-grey lighten-1"
          ><i class="material-icons right">delete</i>Delete</a
        >
        <h3><%= manufacturer.name %></h3>
        <p><strong>Location: </strong> <%= manufacturer.location %></p>
        <p><%= manufacturer.description %></p>
      </div>
      <div class="row">
        <% if (data.length > 0){ %>
        <p>
          List of mech part instances delivered by <%= manufacturer.name %>:
        </p>
        <table class="striped centered">
          <thead>
            <th>SerialNo</th>
            <th>Status</th>
            <th>Date Received</th>
            <th>Storage</th>
            <th>Client</th>
            <th>Mech Part Instance Detail</th>
          </thead>
          <tbody>
            <% for (let mech of data){ %>
            <tr>
              <td><%= mech.serialNo %></td>
              <td>
                <% if (mech.status === `Available`) { %>
                <span class="light-green-text text-darken-1"
                  ><%= mech.status %></span
                >
                <% } else if (mech.status === `Reserved`) { %>
                <span class="yellow-text text-darken-2"
                  ><%= mech.status %></span
                >
                <% } else { %>
                <span class="red-text text-darken-1"><%= mech.status %></span>
                <% } %>
              </td>
              <td><%= mech.dateReceived_formatted %></td>
              <td>
                <a href="<%= mech.storage.url %>"><%= mech.storage.name %></a>
              </td>
              <td>
                <% if (mech.client) { %>
                <a href="<%= mech.client.url %>"><%= mech.client.name %></a>
                <% } else { %> - <% } %>
              </td>
              <td><a href="<%= mech.url %>">Link</a></td>
            </tr>
            <% } %>
          </tbody>
        </table>
        <% } else { %>
        <p>
          There are no mechs supplied by <%= manufacturer.name %> to the shop.
        </p>
        <% } %>
      </div>
    </div>
    <%- include('footer'); -%>
  </body>
</html>
