<!DOCTYPE html>
<html>
  <head>
    <title><%= title %> - <%= client_data.name %></title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
  </head>
  <body>
    <%- include('header'); -%>
    <div class="container">
      <h1><%= title %></h1>

      <div class="row">
        <div class="col s12">
          <% if (client_mechs_data.length > 0){ %>
          <p>
            Delete the following list of Mech Part Instances before attempting
            to delete <strong>"<%= client_data.name %>"</strong>
          </p>
          <br />
          <table class="striped centered">
            <thead>
              <th>Serial No.</th>
              <th>Status</th>
              <th>Mech Part Instance Detail</th>
            </thead>
            <tbody>
              <% for (let mech of client_mechs_data){ %>
              <tr>
                <td><%= mech.serialNo %></td>
                <td>
                  <% if (mech.status === `Available`) { %>
                  <span class="light-green-text text-darken-1"
                    ><%= mech.status %></span
                  >
                  <% } else if (mech.status === `Reserved`) { %>
                  <span class="yellow-text text-darken-2"
                    ><%= mech.status %></span
                  >
                  <% } else { %>
                  <span class="red-text text-darken-1"><%= mech.status %></span>
                  <% } %>
                </td>
                <td><a href="<%= mech.url %>">Link</a></td>
              </tr>
              <% } %>
            </tbody>
          </table>
          <% } else { %>
          <p>
            Do you really want to delete
            <strong>"<%= client_data.name %>"</strong>?
          </p>
          <form method="POST">
            <div class="input-field">
              <input
                id="client_id"
                name="client_id"
                type="hidden"
                value="<%= client_data._id %>"
              />
            </div>
            <button
              class="btn waves-effect waves-light"
              type="submit"
              name="action"
            >
              Delete
              <i class="material-icons right">send</i>
            </button>
          </form>
          <% } %>
        </div>
      </div>
    </div>
    <%- include('footer'); -%>
  </body>
</html>
