<!DOCTYPE html>
<html>
  <head>
    <title><%= title %>: <%= data.name %></title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
  </head>
  <body>
    <%- include('header'); -%>
    <div class="container">
      <div class="row">
        <h1><%= title %>: <%= data.name %></h1>
        <div class="row">
          <div class="col s12">
            <% if (mech_list_data.length > 0){ %>
            <p>
              Delete the following list of Mechs in the <%= data.name %> class
              before attempting to delete <%= data.name %> class
            </p>
            <hr />
            <br />
            <table class="striped centered">
              <thead>
                <th>Model</th>
                <th>Name</th>
                <th>Mech Detail</th>
              </thead>
              <tbody>
                <% for (let mech of mech_list_data){ %>
                <tr>
                  <td><%= mech.model %></td>
                  <td><%= mech.name %></td>
                  <td><a href="<%= mech.url %>">Link</a></td>
                </tr>
                <% } %>
              </tbody>
            </table>
            <% } else { %>
            <p>Do you really want to delete <%= data.name %> class?</p>
            <form method="POST">
              <div class="input-field">
                <input
                  id="category_id"
                  name="category_id"
                  type="hidden"
                  value="<%= data._id %>"
                />
              </div>
              <button
                class="btn waves-effect waves-light"
                type="submit"
                name="action"
              >
                Delete
                <i class="material-icons right">send</i>
              </button>
            </form>
            <% } %>
          </div>
        </div>
      </div>
    </div>
    <%- include('footer'); -%>
  </body>
</html>
